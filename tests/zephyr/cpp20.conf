# SPDX-License-Identifier: Apache-2.0

# Keep the C++ standard selection in a later-merged fragment. In this Zephyr
# tree, the choice assignment is dropped when placed directly in prj.conf.
CONFIG_STD_CPP20=y
